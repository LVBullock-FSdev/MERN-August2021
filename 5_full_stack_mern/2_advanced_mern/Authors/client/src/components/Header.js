import React from 'react';
import { Link} from '@reach/router';

const Header = (props) =>{

    return(
        <header>
        <h1>Favorite Authors</h1>
        </header>
    )
}

export default Header;